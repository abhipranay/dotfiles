return {
  "nvim-telescope/telescope.nvim",
  keys = {
    -- disable the keymap to grep files
    { "<leader>/", false },
  },
  opts = {
    defaults = {
      -- path_display = { "smart" },
    },
  },
}
